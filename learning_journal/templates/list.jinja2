{% estends 'base.jinja2' %}
{% block content%}
    <article class="entries">
        {% for entry in entries %}
        <a href="{{ entry.id }}">{{ entry.title }}</a>
        <p>{{ entry.created }}</p>
        {% endfor %}
    </article>
{% endblock content %}
