{% extends 'base.jinja2' %}
{% block content%}
    <article class="entries">
        {% for entry in entries %}
        <a href="/entries/{{ entry.id }}">{{ entry.title }}</a>
        <p>{{ entry.created }}</p>
        {% endfor %}
        <a href="/add">Add New</a>
    </article>
{% endblock %}
